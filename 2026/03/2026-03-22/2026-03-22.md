# [1886. 判断矩阵经轮转后是否一致](https://leetcode.cn/problems/determine-whether-matrix-can-be-obtained-by-rotation/)

**2026-03-22**

**题面难度：Easy** 

[查看PDF题面](./2026-03-22.png)

---

## 翻转矩阵

顺时针旋转 90 度：先沿主对角线翻转，再沿中线翻转

检查 0，90，180，270  四种状态

---

## 代码实现 (C++)
```cpp
class Solution {
public:
    bool findRotation(vector<vector<int>>& mat, vector<vector<int>>& target) {
        int n=mat.size();
        function<bool()>check=[&]()->bool{
            for(int i=0;i<n;++i){
                for(int j=0;j<n;++j){
                    if(mat[i][j]!=target[i][j]){
                        return 0;
                    }
                }
            }
            return 1;
        };
        function<void()>re=[&]()->void{
            for(int i=0;i<n;++i){
                for(int j=i+1;j<n;++j){
                    swap(mat[i][j],mat[j][i]);
                }
            }
            for(int i=0;i<n;++i){
                int l=0,r=n-1;
                while(l<r){
                    swap(mat[i][l++],mat[i][r--]);
                }
            }
        };
        if(check())return 1;
        re();
        if(check())return 1;
        re();
        if(check())return 1;
        re();
        if(check())return 1;
        return 0;
    }
};
```

- 时间复杂度: $O(N^2)$
- 空间复杂度: $O(1)$

---

[每日一题记录](https://github.com/kwssbt/Leetcode-daily-question)

