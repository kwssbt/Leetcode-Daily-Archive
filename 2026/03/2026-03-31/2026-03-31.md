# [3474. 字典序最小的生成字符串](https://leetcode.cn/problems/lexicographically-smallest-generated-string/)

**2026-03-31**

**题面难度：Hard** 

[查看PDF题面](./2026-03-31.png)

---

## 构造&贪心

先尝试将所有 `str1[i]='T'` 对应的子串填好，如果遇到冲突返回空串

再检查所有 `str1[i]='F'` 对应的子串

如果已有的字符已经保证不同，则全部填成 `'a'`

否则将最后一个空余位置填成不同的字符，若没有空余的位置返回空串

最后将所有空余位置填成 `'a'`


---

## 代码实现 (C++)
```cpp
class Solution {
public:
    string generateString(string str1, string str2) {
        int n=str1.size();
        int m=str2.size();

        string s(n+m-1,'#');

        for(int i=0;i<n;++i){
            if(str1[i]=='T'){
                for(int j=0;j<m;++j){
                    if(s[i+j]==str2[j]){
                        continue;
                    }
                    if(s[i+j]=='#'){
                        s[i+j]=str2[j];
                    }
                    else{
                        return "";
                    }
                }
            }
        }
        
        for(int i=0;i<n;++i){
            if(str1[i]=='F'){
                bool need=1;
                for(int j=m-1;need&&j>=0;--j){
                    if(s[i+j]!='#'){
                        if(s[i+j]!=str2[j]){
                            need=0;
                        }
                    }
                    else{
                        if(str2[j]!='a'){
                            need=0;
                        }
                    }
                }
                if(need){
                    bool f=1;
                    for(int j=m-1;f&&j>=0;--j){
                        if(s[i+j]=='#'){
                            s[i+j]=(str2[j]=='a'?'b':'a');
                            f=0;
                        }
                    }
                    if(f){
                        return "";
                    }
                }
            }
        }

        for(int i=0;i<s.size();++i){
            if(s[i]=='#'){
                s[i]='a';
            }
        }

        return s;
    }
};
```

- 时间复杂度: $O(NM)$
- 空间复杂度: $O(N+M)$

---

[每日一题记录](https://github.com/kwssbt/Leetcode-daily-question)

